<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Tech Store</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #f0f2f5; margin: 0; padding: 40px; }
        h1 { text-align: center; color: #333; }
        .product-list { display: flex; gap: 25px; flex-wrap: wrap; justify-content: center; margin-top: 30px; }
        .card { 
            background: white; border-radius: 12px; width: 240px; padding: 20px; 
            box-shadow: 0 4px 15px rgba(0,0,0,0.1); text-align: center; transition: transform 0.2s;
        }
        .card:hover { transform: translateY(-5px); }
        .img-container { height: 120px; display: flex; align-items: center; justify-content: center; margin-bottom: 15px; }
        img { max-width: 100px; max-height: 100px; object-fit: contain; }
        h3 { margin: 10px 0; color: #444; font-size: 1.2em; }
        .price { color: #28a745; font-weight: bold; font-size: 1.3em; margin-bottom: 15px; }
        button { 
            background: #007bff; color: white; border: none; padding: 12px; 
            width: 100%; border-radius: 8px; cursor: pointer; font-weight: 600;
        }
        button:hover { background: #0056b3; }
        button i { margin-right: 8px; } /* Space for the icon */
    </style>
</head>
<body>

    <h1><i class="fas fa-store"></i> Welcome to My Store</h1>

    <div class="product-list">
        <% if (products.length > 0) { %>
            <% products.forEach(p => { %>
                <div class="card">
                    <div class="img-container">
    <img src="<%= p.imageUrl %>" 
         onerror="this.src='https://cdn-icons-png.flaticon.com/512/1250/1250615.png'" 
         alt="Product Icon"
         style="width: 150px; height: 150px; object-fit: contain;">
</div>
                    <h3><%= p.title %></h3>
                    <p class="price">$<%= p.price %></p>
                    <button><i class="fas fa-cart-plus"></i> Add to Cart</button>
                </div>
            <% }) %>
        <% } else { %>
            <p>No products found. Add some to your MySQL database!</p>
        <% } %>
    </div>
</body>
</html>